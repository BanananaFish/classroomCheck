<view>
    <view class="progressList" wx:for="{{ progressList }}" wx:key="id">
    <!-- <view wx:if="{{ item.isRender }}"> -->
    <view class="infoForm">
    <view class="flex-row">
        <text class="flex-cell flex-row">姓名</text>
        <text class="flex-cell flex-row">{{ item.name }}</text>
        <text class="flex-cell flex-row">教室</text>
        <text class="flex-cell flex-row">{{ item.classroom }}</text>
    </view>
    <view class="flex-row">
        <text class="flex-cell flex-row">联系方式</text>
        <text class="flex-cell flex-row">{{ item.tel }}</text>
        <text class="flex-cell flex-row">申请日期</text>
        <text class="flex-cell flex-row">{{ item.date }}</text>
    </view>
    <view class="flex-row">
    <text class="flex-cell flex-row">是否通过</text>
        <text class="flex-cell flex-row">{{ item.isPass?'通过':'未通过' }}</text>
    </view>
    </view>
    </view>
</view>